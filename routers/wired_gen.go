// Code generated by cmd/routerdoc; DO NOT EDIT.

package routers

// wired is every hand-written route in this package, with the sentence lifted
// from the Go doc comment on the handler it names. See cmd/routerdoc.
var wired = []wire{
	{"/v1/3d/:id", "GET", "RetrieveThreeD", "Implements GET /v1/3d/:id (Retrieve 3D status).", "Implements GET /v1/3d/:id (Retrieve 3D status)."},
	{"/v1/3d/:id/content", "GET", "ThreeDContent", "Implements GET /v1/3d/:id/content (Download 3D splat/glb content).", "Implements GET /v1/3d/:id/content (Download 3D splat/glb content)."},
	{"/v1/3d/generations", "POST", "ThreeDGenerations", "Implements POST /v1/3d/generations (Text/Image to 3D & Gaussian Splats).", "Implements POST /v1/3d/generations (Text/Image to 3D & Gaussian Splats)."},
	{"/v1/admin/model-access", "GET", "AdminListModelAccess", "Lists the gated-model access rows, filtered to one org by ?owner.", "Lists the gated-model access rows, filtered to one org by\n?owner. SuperAdmin only; without ?owner it returns every org's rows."},
	{"/v1/admin/model-access", "POST", "AdminGrantModelAccess", "Grants — or records a request for — one org or user's access to a gated model.", "Grants — or records a request for — one org or user's access\nto a gated model. SuperAdmin only, at platform scope. The body is {owner, user,\nemail?, model, status?}: an empty user grants the whole org, and status defaults to\n\"granted\"."},
	{"/v1/admin/providers", "GET", "GetAdminProviders", "List admin-owned Model providers as a clean management view (enabled/primary/keyPresent/modelCount).", "List admin-owned Model providers as a clean management view\n(enabled/primary/keyPresent/modelCount). Never returns secret material.\nSuper-admin gated (see routers/authz_filter.go superAdminEndpoints)."},
	{"/v1/admin/providers/primary", "POST", "SetPrimaryAdminProvider", "Make one admin-owned Model provider the primary (IsDefault=true) and clear IsDefault on all other Model providers, so EXACTLY ONE primary exists.", "Make one admin-owned Model provider the primary (IsDefault=true)\nand clear IsDefault on all other Model providers, so EXACTLY ONE primary\nexists. Returns the full updated management list."},
	{"/v1/admin/providers/toggle", "POST", "ToggleAdminProvider", "Enable/disable an admin-owned Model provider (sets State to \"Active\"/\"Disabled\") via the SAME store the generic CRUD uses.", "Enable/disable an admin-owned Model provider (sets State to\n\"Active\"/\"Disabled\") via the SAME store the generic CRUD uses. Persists\nacross restarts (init.go never clobbers State on an existing record).\nReturns the updated management view for that provider."},
	{"/v1/admin/refresh-model-pricing", "POST", "RefreshModelPricing", "Forces a live pricing refresh from the configured pricing service, so the catalogue's rates match the source without waiting for the cycle.", "Forces a live pricing refresh from the configured pricing\nservice, so the catalogue's rates match the source without waiting for the cycle."},
	{"/v1/admin/reload-model-config", "POST", "ReloadModelConfig", "Reloads the model configuration from YAML and refreshes live pricing, so a catalogue change takes effect without a restart.", "Reloads the model configuration from YAML and refreshes live\npricing, so a catalogue change takes effect without a restart."},
	{"/v1/admin/usage/backfill-do", "POST", "PostBackfillDOUsage", "Super-admin: backfill the hanzo.cloud_usage ledger from DigitalOcean's billing API for (day) windows our native metering missed — GAPS ONLY (never double-counts a natively-metered day), PROVENANCE-tagged (source=\"do-backfill\"), and IDEMPOTENT (re-running is a no-op).", "Super-admin: backfill the hanzo.cloud_usage ledger from DigitalOcean's\nbilling API for (day) windows our native metering missed — GAPS ONLY (never\ndouble-counts a natively-metered day), PROVENANCE-tagged (source=\"do-backfill\"),\nand IDEMPOTENT (re-running is a no-op). DRY-RUN by default: returns the rows/days/\ntotals it WOULD write and persists nothing unless dryRun=false is passed explicitly."},
	{"/v1/ai/connections", "GET", "GetAIConnections", "Lists the org's connectable AI accounts and whether each is currently connected.", "Lists the org's connectable AI accounts and whether each is\ncurrently connected. Never returns a key or a kms:// reference."},
	{"/v1/ai/connections", "POST", "AddAIConnection", "Connects (or reconnects) a third-party AI account for the org by sealing the supplied key into KMS and upserting the org's provider row.", "Connects (or reconnects) a third-party AI account for the org by\nsealing the supplied key into KMS and upserting the org's provider row. The raw\nkey is sealed BEFORE the row is built and is never persisted or echoed."},
	{"/v1/ai/connections/:provider", "DELETE", "DeleteAIConnection", "Disconnects a third-party AI account: it deactivates the org's row so completion resolution falls back to the global Hanzo account (no BYO), and best-effort tombstones the sealed secret.", "Disconnects a third-party AI account: it deactivates the org's\nrow so completion resolution falls back to the global Hanzo account (no BYO), and\nbest-effort tombstones the sealed secret. Idempotent."},
	{"/v1/ai/connections/:provider", "POST", "DeleteAIConnection", "Disconnects a third-party AI account: it deactivates the org's row so completion resolution falls back to the global Hanzo account (no BYO), and best-effort tombstones the sealed secret.", "Disconnects a third-party AI account: it deactivates the org's\nrow so completion resolution falls back to the global Hanzo account (no BYO), and\nbest-effort tombstones the sealed secret. Idempotent."},
	{"/v1/ai/connections/:provider/authorize", "GET", "ConnectAIProvider", "Begins an OAuth connection for the caller's org: it binds the org into a signed state and sends the caller to the provider's authorize URL.", "Begins an OAuth connection for the caller's org: it binds the\norg into a signed state and sends the caller to the provider's authorize URL. By\ndefault it 302-redirects (a top-level browser \"connect your login\" click); a\nSPA/BFF that needs to drive the redirect itself passes ?format=json and gets\n{authorizeUrl} in the standard envelope. The org is the VERIFIED principal, so\nonly the caller's own connection can result."},
	{"/v1/ai/connections/:provider/callback", "GET", "CallbackAIProvider", "Completes OAuth: the org is recovered from the SIGNED state (not a header), the code is exchanged for a token, the token is SEALED into KMS (never the row/logs) through the same path as a BYOK key, and the org's provider row is upserted to \"connected\".", "Completes OAuth: the org is recovered from the SIGNED state\n(not a header), the code is exchanged for a token, the token is SEALED into KMS\n(never the row/logs) through the same path as a BYOK key, and the org's provider\nrow is upserted to \"connected\". The browser is then redirected back to the console\nwith ?ai_connected=<provider> (or ?ai_connect_error=<provider> on failure). Because\nthe org comes from the state THIS server signed, an attacker cannot land their\ntoken in a victim org — which is why this endpoint is state-authenticated rather\nthan credential-gated."},
	{"/v1/ai/connections/:provider/usage", "GET", "GetAIConnectionUsage", "Imports the caller org's usage for a connected third-party account.", "Imports the caller org's usage for a connected third-party\naccount. The org is resolved from the VERIFIED principal (requireConnectionOrg), so a\ntenant reads only its own connection. The key is unsealed SERVER-SIDE and never\nreturned. An unconnected account, a missing importer, or a scope-denied provider all\nreturn a 200 ProviderUsage with connected/available flags + a human note — the UI's\nhonest-empty states — never a fabricated figure."},
	{"/v1/ai/feedback", "POST", "AddRoutingReward", "Attaches a per-request outcome reward to the routing decision that served request_id — the enso training loop's quality signal.", "Attaches a per-request outcome reward to the routing decision\nthat served request_id — the enso training loop's quality signal. Org-scoped via\nthe same session-OR-Bearer principal the usage read uses (RequirePrincipal): the\nreward lands only on the caller's OWN org's event, so a request_id from another\norg (or unknown) is a 404 — cross-org writes are impossible and unknown ids are\nindistinguishable from foreign ones. Idempotent: a repeat overwrites. The body\ncarries NO prompt text — only {request_id, reward|rating}."},
	{"/v1/ai/finetune/cancel", "POST", "CancelFinetuneJob", "Deletes the TrainJob CR, meters the GPU-hours used so far, and marks the job cancelled.", "Deletes the TrainJob CR, meters the GPU-hours used so far, and\nmarks the job cancelled. ?id= or ?name="},
	{"/v1/ai/finetune/deploy", "POST", "DeployFinetuneJob", "Serves a completed job's checkpoints and registers the result as a routable model on api.hanzo.ai.", "Serves a completed job's checkpoints and registers the result\nas a routable model on api.hanzo.ai. ?id= or ?name="},
	{"/v1/ai/finetune/hf/datasets", "GET", "SearchHfDatasets", "Proxies a HuggingFace dataset search (dataset picker).", "Proxies a HuggingFace dataset search (dataset picker)."},
	{"/v1/ai/finetune/hf/models", "GET", "SearchHfModels", "Proxies a HuggingFace model search (base-model picker).", "Proxies a HuggingFace model search (base-model picker)."},
	{"/v1/ai/finetune/hf/repo", "GET", "GetHfRepo", "Returns a repo's detail (files, gated/private state).", "Returns a repo's detail (files, gated/private state). ?id=&kind=model|dataset"},
	{"/v1/ai/finetune/job", "GET", "GetFinetuneJob", "Returns one job with refreshed live status.", "Returns one job with refreshed live status. ?id=owner/name or ?name="},
	{"/v1/ai/finetune/jobs", "GET", "ListFinetuneJobs", "Returns the org's jobs, refreshing live status for active ones.", "Returns the org's jobs, refreshing live status for active ones."},
	{"/v1/ai/finetune/jobs", "POST", "CreateFinetuneJob", "Validates the request, resolves efficient defaults, persists the job, and submits a real TrainJob CR.", "Validates the request, resolves efficient defaults, persists the\njob, and submits a real TrainJob CR. A submit failure (e.g. no cluster wired) is\nsurfaced honestly: the job is saved with status \"failed\" + the reason, never faked."},
	{"/v1/ai/finetune/presets", "GET", "GetFinetunePresets", "Returns the new-job catalog plus, when a selection is passed (?baseModel&method&task&preset[&datasetExamples]), the recommended config so the console can render \"Recommended\" as a one-click, ready-to-run default.", "Returns the new-job catalog plus, when a selection is passed\n(?baseModel&method&task&preset[&datasetExamples]), the recommended config so the\nconsole can render \"Recommended\" as a one-click, ready-to-run default."},
	{"/v1/ai/memory/delete", "POST", "MemoryDelete", "Delete one of the authenticated user's memories", "Delete one of the authenticated user's memories"},
	{"/v1/ai/memory/facts", "GET", "MemoryFacts", "List the authenticated user's stored facts", "List the authenticated user's stored facts"},
	{"/v1/ai/memory/list", "GET", "MemoryList", "List the authenticated user's memories, newest first", "List the authenticated user's memories, newest first"},
	{"/v1/ai/memory/recall", "GET", "MemoryRecall", "Recall recent/relevant memories for context injection; with q it", "Recall recent/relevant memories for context injection; with q it\nranks semantically, without q it returns the most recent"},
	{"/v1/ai/memory/remember", "POST", "MemoryRemember", "Store a memory for the authenticated user", "Store a memory for the authenticated user"},
	{"/v1/ai/memory/search", "GET", "MemorySearch", "Search the authenticated user's memories (semantic, text fallback)", "Search the authenticated user's memories (semantic, text fallback)"},
	{"/v1/ai/memory/update", "POST", "MemoryUpdate", "Update one of the authenticated user's memories", "Update one of the authenticated user's memories"},
	{"/v1/ai/org/settings", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/org/settings/list", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/rag/context", "GET", "RagContext", "Return every stored chunk of one file_id (full document context).", "Return every stored chunk of one file_id (full document context).\nConsolidates the retired chat-rag-api GET /documents/{id}/context."},
	{"/v1/ai/rag/delete", "POST", "RagDelete", "Delete all chunks of one or more uploaded files (by file_id) from the owner's Search+Vector index.", "Delete all chunks of one or more uploaded files (by file_id) from\nthe owner's Search+Vector index. Consolidates the retired chat-rag-api\nDELETE /documents."},
	{"/v1/ai/rag/embed", "POST", "RagEmbed", "Parse, chunk, and embed one uploaded file under its file_id into the unified Search+Vector index, scoped to the authenticated owner.", "Parse, chunk, and embed one uploaded file under its file_id into\nthe unified Search+Vector index, scoped to the authenticated owner. Provide\ninline `content` or a `url` to fetch+parse (PDF/CSV/XLSX/PPTX/…). Re-embedding\nthe same file_id replaces its chunks. Consolidates the retired chat-rag-api\nPOST /embed and /local/embed."},
	{"/v1/ai/rag/ingest", "POST", "IngestDocs", "Unified RAG ingest: parse + chunk + embed documents and pipe them to BOTH Hanzo Vector (semantic) AND Hanzo Search (keyword) under the tenant index {owner}-{store}-docs — the same index /v1/chat retrieval reads.", "Unified RAG ingest: parse + chunk + embed documents and pipe them\nto BOTH Hanzo Vector (semantic) AND Hanzo Search (keyword) under the tenant\nindex {owner}-{store}-docs — the same index /v1/chat retrieval reads. The\nsource is pluggable: \"upload\" (inline files/documents), \"github\" (index a\nrepo), \"crawl\" (web), or \"s3\" (the store's object-storage space). The owner\nis bound to the authenticated principal; the client-supplied owner is never\ntrusted."},
	{"/v1/ai/rag/query", "POST", "RagQuery", "Retrieve the top-K chunks relevant to a query, scoped to a single uploaded file (`file_id`).", "Retrieve the top-K chunks relevant to a query, scoped to a single\nuploaded file (`file_id`). Hybrid keyword+vector retrieval over the same\nindex. Consolidates the retired chat-rag-api POST /query."},
	{"/v1/ai/rag/query-multiple", "POST", "RagQueryMultiple", "Retrieve the top-K chunks relevant to a query, scoped to a SET of uploaded files (`file_ids`).", "Retrieve the top-K chunks relevant to a query, scoped to a SET of\nuploaded files (`file_ids`). Consolidates the retired chat-rag-api POST\n/query_multiple. Shares one retrieval path with /rag/query."},
	{"/v1/ai/router/artifact-meta", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/router/defaults", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/router/history", "GET", "GetRouterHistory", "Returns the router-improvement time-series.", "Returns the router-improvement time-series. Two scopes, one route,\nmirroring /v1/ai/router/stats:\n\n  - ?scope=platform — PUBLIC-safe aggregate over ALL orgs, no authentication. Emits\n    the daily reward/cost-saved/adoption series (task mix included, model ids NOT)\n    and the retrain timeline. This is what world.hanzo.ai polls.\n  - default (org scope) — requires a signed-in principal, scoped to the caller's OWN\n    org (a super admin may pass ?org= to target another or \"\" for all).\n\nWindow: ?days=N (default 30, capped at 90). Aggregates only."},
	{"/v1/ai/router/judge-panel", "GET", "GetRouterJudgePanel", "Returns the LIVE Mean-Field Judge Panel state: the configured panel + dynamic judge posture (enabled/sample) resolved from the \"*\" GlobalDefaultOwner row, the live in-process per-judge calibration (weight/mean/n), and the static published benchmark.", "Returns the LIVE Mean-Field Judge Panel state: the configured\npanel + dynamic judge posture (enabled/sample) resolved from the \"*\"\nGlobalDefaultOwner row, the live in-process per-judge calibration (weight/mean/n),\nand the static published benchmark. PUBLIC-safe and platform-global (model ids +\nscalars only), so it rides the same unauthenticated, balance-exempt class as\n/v1/ai/router/stats?scope=platform — the world widget polls it with no auth. The judge\nstate is a single in-process population (not per-org), so there is nothing to scope;\n?scope=platform is accepted for symmetry with router-stats."},
	{"/v1/ai/router/ledger", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/router/policy", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/router/rewards", "*", "RouterConfigBridge", "The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and /v1/ai/org/settings[/list]).", "The HTTP transport binding for the RESTful router-config\nnouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and\n/v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —\nthe SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200\nhandler serves over the gateway transport. The native ZAP\nhandler is the ONE and ONLY implementation of these routes; this is purely the\napi.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the\nsplit-brain the router refactor removed stays removed.\n\nWhy a bridge and not a twin controller method: every other migrated route\n(get-records, get-connections, …) carries BOTH a controller method and a\nZAP handler — the exact dual-impl drift that silently NULLed customer router\nsettings (the update-router-policy data-wipe). Routing these nouns through the\nZAP handler over one adapter keeps a single source of truth.\n\nIdentity is the request's own Bearer credential (Authorization header), which\nthe native handlers resolve exactly as the gateway does — every caller\n(console, chat, app) already sends it. The dispatched handler returns a ZAP\nmessage whose status is field 0 and body is field 4 (BuildCloudResponse /\nBuildGatewayResponse layout); both are relayed verbatim. The route is mapped\n\"*\" (any verb) because the native handler is method-aware: /v1/ai/router/policy\nsplits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns\n405 for a verb it does not own."},
	{"/v1/ai/router/stats", "GET", "GetRouterStats", "Returns the router observability aggregate.", "Returns the router observability aggregate. Two scopes, one route:\n\n  - ?scope=platform — PUBLIC-safe aggregate over ALL orgs, no authentication.\n    Emits rates, shares, per-task/per-model counts, throughput, and the cost\n    RATIO (saved_pct) + counterfactual model id, but NEVER absolute $ levels,\n    org identity, raw events, or feature vectors. This is what world.hanzo.ai\n    polls.\n  - default (org scope) — requires a signed-in principal; scoped to the caller's\n    OWN org (a super admin may pass ?org= to target another org or \"\" for all).\n    Carries the absolute $/MTok indices for the admin savings panel.\n\nWindow: ?since= (RFC3339) or ?hours= (default 24, capped). Aggregates only."},
	{"/v1/ai/traffic/globe", "GET", "GetTrafficGlobe", "Returns the PUBLIC live request-geo aggregate for the world.hanzo.ai \"Hanzo mode\" globe: WHERE requests to api.hanzo.ai are coming from, as country/region points with per-service-class counts, plus headline throughput rates.", "Returns the PUBLIC live request-geo aggregate for the\nworld.hanzo.ai \"Hanzo mode\" globe: WHERE requests to api.hanzo.ai are coming from,\nas country/region points with per-service-class counts, plus headline throughput\nrates.\n\nIt is AUTH-exempt and BALANCE-exempt exactly like /v1/ai/router/stats?scope=platform:\n  - auth: the controller name \"traffic/globe\" is neither a get-/update- CRUD name\n    nor a super-admin/present-credential endpoint, so the authz filter passes it\n    through, and this handler requires no principal.\n  - balance: isBalanceExempt(\"/v1/ai/traffic/...\") returns true.\n\nIt exposes ONLY aggregates — counts, rates, and country/region centroids — and\nNEVER any IP, per-request row, org, or user dimension (see object/traffic.go).\nMarketing telemetry; nothing sensitive."},
	{"/v1/audio/foley", "POST", "AudioMedia", "Serves the generative audio verbs — /v1/audio/voice (TTS), /music, /foley — that the Zen family serves natively.", "Serves the generative audio verbs — /v1/audio/voice (TTS), /music,\n/foley — that the Zen family serves natively. It resolves the SKU and, for a Zen\nmodel, forwards to zen's matching verb billed per call at the discovered price.\nThese verbs are Zen-native; a non-Zen model is rejected."},
	{"/v1/audio/music", "POST", "AudioMedia", "Serves the generative audio verbs — /v1/audio/voice (TTS), /music, /foley — that the Zen family serves natively.", "Serves the generative audio verbs — /v1/audio/voice (TTS), /music,\n/foley — that the Zen family serves natively. It resolves the SKU and, for a Zen\nmodel, forwards to zen's matching verb billed per call at the discovered price.\nThese verbs are Zen-native; a non-Zen model is rejected."},
	{"/v1/audio/speech", "POST", "AudioSpeech", "The OpenAI-compatible TTS endpoint (POST /v1/audio/speech).", "The OpenAI-compatible TTS endpoint (POST /v1/audio/speech). It\nauthenticates the caller, resolves `model` to its TTS provider (the SAME model-route\nresolution the chat/images/video endpoints use — so a BYO node registered as a TTS\nprovider works transparently), synthesizes the audio, and streams the bytes back.\nThis is the ONE way to synthesize speech: OpenAI-shaped, with no store or\nmessage coupling, so a caller needs no chat to speak."},
	{"/v1/audio/transcriptions", "POST", "AudioTranscriptions", "The OpenAI-compatible STT endpoint (POST /v1/audio/transcriptions, multipart: file + model [+ language + response_format]).", "The OpenAI-compatible STT endpoint\n(POST /v1/audio/transcriptions, multipart: file + model [+ language +\nresponse_format]). It mirrors AudioSpeech exactly: authenticate the caller,\nresolve `model` to its STT provider through the SAME model-route resolution\n(so the in-cluster speech service — or any BYO node registered as an STT\nprovider — works transparently), transcribe, and return the OpenAI body.\nThis is the ONE way to transcribe: OpenAI-shaped, with no store coupling, so a\ncaller needs no chat to be heard."},
	{"/v1/audio/voice", "POST", "AudioMedia", "Serves the generative audio verbs — /v1/audio/voice (TTS), /music, /foley — that the Zen family serves natively.", "Serves the generative audio verbs — /v1/audio/voice (TTS), /music,\n/foley — that the Zen family serves natively. It resolves the SKU and, for a Zen\nmodel, forwards to zen's matching verb billed per call at the discovered price.\nThese verbs are Zen-native; a non-Zen model is rejected."},
	{"/v1/chat", "POST", "ChatCompletions", "Implements the OpenAI-compatible chat completions API", "Implements the OpenAI-compatible chat completions API"},
	{"/v1/chat/completions", "POST", "ChatCompletions", "Implements the OpenAI-compatible chat completions API", "Implements the OpenAI-compatible chat completions API"},
	{"/v1/chat/public", "POST", "ChatCompletionsPublic", "Serves one completion to a caller with no account.", "Serves one completion to a caller with no account."},
	{"/v1/completions", "POST", "ChatCompletions", "Implements the OpenAI-compatible chat completions API", "Implements the OpenAI-compatible chat completions API"},
	{"/v1/embeddings", "POST", "Embeddings", "Implements POST /v1/embeddings (OpenAI-compatible).", "Implements POST /v1/embeddings (OpenAI-compatible).\n\nBody: {\"model\": \"...\", \"input\": \"...\"|[\"...\", ...], \"encoding_format\"?, \"dimensions\"?}\nIt authenticates the caller, resolves the model to its upstream provider via\nthe shared routing table, rewrites the user-facing model name to the upstream\nid, and proxies the request to the provider's /embeddings endpoint verbatim."},
	{"/v1/images/generations", "POST", "ImagesGenerations", "Implements POST /v1/images/generations (OpenAI-compatible).", "Implements POST /v1/images/generations (OpenAI-compatible).\n\nBody: {\"model\": \"...\", \"prompt\": \"...\", \"n\"?: int, \"size\"?: \"1024x1024\",\n\n\t\"response_format\"?: \"url\"|\"b64_json\"}\n\nIt authenticates the caller, resolves the model to its upstream provider via\nthe shared routing table (zen3-image* → do-ai fal diffusion), reserves the\nper-image budget, generates the image(s) through the do-ai async image\nclient, records usage for billing, and returns the OpenAI images response."},
	{"/v1/index", "POST", "IndexDocs", "Index documentation into Meilisearch and Qdrant", "Index documentation into Meilisearch and Qdrant"},
	{"/v1/messages", "POST", "AnthropicMessages", "Implements the Anthropic Messages API.", "Implements the Anthropic Messages API."},
	{"/v1/messages/count_tokens", "POST", "AnthropicCountTokens", "Implements POST /v1/messages/count_tokens.", "Implements POST /v1/messages/count_tokens. Claude Code\ncalls it before a request; it returns {\"input_tokens\": N} for the given\nmodel + messages + tools."},
	{"/v1/metrics", "GET", "GetMetrics", "Get Prometheus metrics", "Get Prometheus metrics"},
	{"/v1/models", "GET", "ListModels", "Returns the list of available models from the routing table.", "Returns the list of available models from the routing table.\n\nPUBLIC BY DESIGN, AND IT DOES NOT AUTHENTICATE — that is the whole contract, so it\nis stated here rather than left to be inferred. The catalogue is the same for\neveryone (listAvailableModels takes no principal), docs.hanzo.ai fetches it from\nthe browser, and every policy layer around it already says so out loud: the authz\nfilter lists \"models\" as public, filter_balance refuses to gate it (a 402 here was\na console-wide outage), the rate limiter excludes it, and cloud's spend.Reachable\ncarries /v1/models/ as \"the model catalog the shell reads for discovery\".\n\nSO THE Authorization HEADER IS NOT AN ADMISSION CHECK HERE. It is read for ONE\nthing — annotating gated SKUs with the caller's own access standing — and\nannotation degrades to nothing when there is no verified principal.\n\nIt used to hold a \"require authentication\" gate that authenticated nobody: it\nrejected an ABSENT credential and a MALFORMED one, then accepted any string that\nmerely looked like a key. `Bearer sk-` followed by 36 zeroes returned 200 in\nproduction; so did a JWT three days expired. It was a shape check wearing an auth\ncheck's clothes, and its cost was diagnostic: /v1/models is the natural \"is my auth\nworking?\" probe, and answering 200 to a dead credential sent people debugging the\nwrong system. A public endpoint must not appear to validate. Either check the\ncredential or ignore it — this one ignores it, deliberately and visibly.\n\nRemoving that gate discloses nothing new: the catalogue was already reachable by\nanyone willing to type three characters, so there is no confidentiality delta, only\nan honesty one."},
	{"/v1/models/:model/access", "GET", "GetModelAccessStatus", "Returns the caller's own standing for a gated model: \"granted\", \"requested\", or empty when they have never asked.", "Returns the caller's own standing for a gated model:\n\"granted\", \"requested\", or empty when they have never asked."},
	{"/v1/models/:model/access", "POST", "RequestModelAccess", "Records the caller's waitlist request for a gated model and answers their new standing.", "Records the caller's waitlist request for a gated model and\nanswers their new standing. Authed, idempotent, and self-scoped: the row is keyed\nto the caller's own org and identity, never to a body-supplied owner."},
	{"/v1/models/providers", "GET", "GetModelProviders", "Public, secret-free list of the providers serving the models that GET /v1/models lists — the same source, projected.", "Public, secret-free list of the providers serving the models that\nGET /v1/models lists — the same source, projected. Safe unauthenticated: no\nkeys, URLs, or config are returned, and it reports a SET of names, never\nwhich provider serves which model."},
	{"/v1/rerank", "POST", "Rerank", "Implements POST /v1/rerank (Cohere/Jina-compatible).", "Implements POST /v1/rerank (Cohere/Jina-compatible).\n\nBody: {\"model\": \"...\", \"query\": \"...\", \"documents\": [\"...\", ...]|[{\"text\":\"...\"}],\n\n\t\"top_n\"?: int, \"return_documents\"?: bool}\n\nResponse: {\"object\":\"list\",\"model\":...,\"results\":[{\"index\",\"relevance_score\",\"document\"?}],\"usage\":{...}}\n\nBackend selection is provider-driven (one endpoint, one contract):\n  - If the model routes to a native rerank provider (Jina/Cohere/Voyage) the\n    request is proxied to that provider's /rerank endpoint.\n  - Otherwise scores are computed as a real bi-encoder ranking: embed the\n    query and documents through the resolved embedding model and rank by\n    cosine similarity. No rerank-specific key required."},
	{"/v1/responses", "POST", "Responses", "Implements POST /v1/responses.", "Implements POST /v1/responses. The converted request is completed by\nthe chat path, which is handed a sink saying where the answer goes: a stream is\ntranslated as it is produced, a whole body is translated entire."},
	{"/v1/search", "POST", "SearchDocs", "Search documentation using hybrid fulltext + vector search", "Search documentation using hybrid fulltext + vector search"},
	{"/v1/search/stats", "GET", "SearchDocsStats", "Get search index statistics", "Get search index statistics"},
	{"/v1/videos/:id", "GET", "RetrieveVideo", "Implements GET /v1/videos/{id} — poll a job's status.", "Implements GET /v1/videos/{id} — poll a job's status.\n\nIt authenticates the caller, verifies they OWN the job (the caller's billing\nsubject must equal the job's), performs ONE upstream status poll, and — the\nfirst time the job is observed completed — settles the reservation with the\nactual cost and records the billable usage event (exactly once). Returns the\nOpenAI-shaped video object."},
	{"/v1/videos/:id/content", "GET", "VideoContent", "Implements GET /v1/videos/{id}/content — download the finished MP4.", "Implements GET /v1/videos/{id}/content — download the finished MP4.\n\nIt authenticates + ownership-checks the caller, then proxies the upstream\n/content endpoint (bounded by the download concurrency ceiling) and streams the\nraw video bytes back inline. A successful download also bills the job once (for\nthe client that downloads without first polling to completion) — idempotent\nwith the poll path via job.markCompleted."},
	{"/v1/videos/generations", "POST", "VideosGenerations", "Implements POST /v1/videos/generations — the ASYNC create.", "Implements POST /v1/videos/generations — the ASYNC create.\n\nBody: {\"model\": \"...\", \"prompt\": \"...\", \"size\"?: \"1280x720\", \"seconds\"?: int}\n\nIt authenticates the caller, resolves the model to its upstream provider via\nthe shared routing table (zen3-video* / wan2-2-t2v-a14b → the spark-video\nbackend), reserves the per-video budget (the balance gate), creates ONE\nupstream job, registers it in the in-pod store, and returns the OpenAI-shaped\nvideo object with status \"queued\" IMMEDIATELY. The client then polls\nGET /v1/videos/{id} and downloads GET /v1/videos/{id}/content. Nothing is\nbilled here — the debit lands on completion."},
}
